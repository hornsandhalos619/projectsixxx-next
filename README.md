# projectsixxx-next

Next.js SAMPLE for [projectsixxx.com](https://projectsixxx.com).

House site (App Router) with the journal route at `/overmind/journal`. Deploy this repo on Vercel to claim the domain. Do not commit secrets or `.env` files.
